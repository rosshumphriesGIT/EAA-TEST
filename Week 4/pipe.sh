#!/bin/bash

#grep searches for a patten in a line of text my sql
# ps -ef prints a snapshot of every process in the system 
#where does the txt comefrom? from the ps -ef |

#this new command:
#searches all the running processes looking for the mysql process
ps -ef | grep cntlm
